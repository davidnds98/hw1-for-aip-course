title: HW1 for AIP
description: HW for AIP
template:
  repo: hw1-for-aip-template
  name: hw1-for-aip
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
